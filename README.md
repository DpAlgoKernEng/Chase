# Chase

A C++ project built with CMake and vcpkg.

## Prerequisites

- CMake 3.19+
- Ninja build system
- vcpkg

## Build

```bash
# Configure
cmake --preset=<preset-name>

# Build
cmake --build --preset=<preset-name>
```

Available presets can be found in `CMakePresets.json`.